#include <stdio.h>
#include <stdlib.h>

main()
{
    int choice;

    puts("Ente the choice");
    scanf("%d",&choice);
    
/*
    switch(3)
    {
        case 1:
            puts("one");
            break;
        case 2:
            puts("two");
            break;
        case 3:
            puts("three");
            break;
        default:
            puts("default");
            break;
    }

    if(choice == 1)
        puts("choice one");

    else if(choice == 2)
        puts("choice two");
        
    else if(choice == 3)
        puts("choice three");
        
    else if(choice == 4)
        puts("choice four");
        
    else 
        puts("choice is default");
        


    if(choice < 10)
        choice = 100;
    else 
        choice = 200;
    
    printf("After the value of choice = %d\n",choice);



    choice = (choice < 222) ?  1111 :  2222;

    printf("choice = %d\n", choice);



    int i = 0;

    do
    {
        puts("Hello one");
        i++;
    }while(i <= choice);
*/

 

    if(choice == 2)
        puts("Hello");
    
}