/* Trial and Error code */
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    char name[10];
    puts("Enter the name : ");
    gets(&name); 
    printf("entered name is %s\n",name);

    char value;
    puts("Ener the number :");
    gets(&value);
    printf("entered number is %c \n",value);
    

}